# ANTHROPIC_API_KEY is loaded directly by annotate-genres from .env
# Do not export it here — it would conflict with claude.ai auth in Claude Code.
# The oh-my-zsh dotenv plugin auto-loads .env on cd; unset it here to prevent the conflict.
unset ANTHROPIC_API_KEY
